# Google ADK Migration Dependencies
# Install with: pip install -r requirements_gemini_adk.txt

# Core ADK framework
google-adk>=1.2.0
google-genai>=1.14.0

# BigQuery (unchanged)
google-cloud-bigquery>=3.40.0

# Observability
langfuse>=3.9.0

# OpenTelemetry (for LangFuse + ADK tracing)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# Utilities
pydantic>=2.11.7
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Data science (used by code execution sandbox)
# Note: These are NOT needed locally since BuiltInCodeExecutor
# runs code server-side in Google's sandbox.

# HTTP client (for gemini grounding tool)
httpx>=0.27.0
backoff>=2.2.1
